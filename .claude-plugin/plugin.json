{
  "name": "memory",
  "version": "1.10.0",
  "description": "Lean memory service for Claude Code: SQLite + sqlite-vec embeddings, FTS, cross-encoder rerank, bi-temporal graph edges, ACT-R activation + active forgetting, cluster-based dedup with MMR extractive merge, untrusted-input filter on read, recall-time staleness flagging for memories citing deleted repo paths, SessionEnd dream hook, compact `search --depth summary` output with `--min-score` filtering, and `memory admin undelete`/`clusters --depth` admin tooling.",
  "homepage": "https://github.com/rkhozinov/memory",
  "license": "MIT",
  "author": {
    "name": "Ruslan Khozinov",
    "url": "https://github.com/rkhozinov"
  }
}
